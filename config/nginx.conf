worker_processes 1;

events {
	worker_connections 100;
}

http {
	access_log off;

	upstream api {
		server api1:3000;
		server api2:3000;
	}

	server {
		listen 9999;

		location / {
			proxy_pass http://api;
		}
	}
}
